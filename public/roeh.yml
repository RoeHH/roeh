--
--Example--
uuid: --automaticly added--
appName: --<AppName>--
description: --<Short description>--
roehDescription: --<description for roeh.ch>--
cloneUrl: --<The http url to clone the repository from>--

--If the Project contains denoModules or deno exe's fill in the folowing. --
denoModules:
  - {
      moduleName: --<moduleName>--,
      description: --<moduleDescription for roeh.ch/x>--,
      importUrl: --<the url to import the deno module from>--,
      requiredFlags: --<flags required to run>--
    }
denoExecutables:
  - {
      executableName: --<executableName>--,
      description: --<executable description for roeh.ch/xx>--,
      runUrl: --<the url to run the deno executable from>--,
      requiredFlags: --<flags required to run>--
    }
